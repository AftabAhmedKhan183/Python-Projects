# Anime Feature Extraction

This project performs feature extraction and preprocessing on an anime dataset using Python. The analysis is implemented in a Jupyter Notebook and uses the `anime.csv` dataset.
